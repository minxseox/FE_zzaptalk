services:
  frontend:
    image: ghcr.io/minxseox/my-expo-web:latest
    container_name: zzaptalk-frontend
    restart: always
    ports:
      - "3000:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
